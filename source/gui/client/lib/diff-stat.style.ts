// The diff stat's measurements, shared with anything that lays a column out
// to hold one: the two figures, then a bar, with a gap either side of it.
export const DIFF_STAT_BAR_WIDTH = 32;
export const DIFF_STAT_GAP = 6;
// What the stat takes beyond its figures.
export const DIFF_STAT_CHROME_PX = DIFF_STAT_BAR_WIDTH + DIFF_STAT_GAP * 2;
